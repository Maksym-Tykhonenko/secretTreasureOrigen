import {scaleSize} from '../utils/scaling';

export const FONTS = {
  InterLight: 'Inter-Light',
  InterRegular: 'Inter-Regular',
  CinzelBold: 'CinzelDecorative-Bold',
};

export const SIZE = {
  extraLarge: scaleSize(32),
  large: scaleSize(24),
  medium: scaleSize(20),
  small: scaleSize(16),
};
